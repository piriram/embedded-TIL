# AI Context — 임베디드 TIL

이 파일을 대화 시작 전에 붙여넣으면 맥락 파악 가능.

---

## 나는 누구

- iOS 개발 경험 있는 주니어 개발자, 임베디드 SW로 전환 중
- 목표: STM32 bare-metal 포트폴리오 완성 → 임베디드/전장 취업

---

## 이 저장소 목적

임베디드 공부하면서 배운 것 마크다운으로 기록. 필기노트 시리즈를 메인으로 두고, 기존 학습자료와 면접대비 자료를 보조로 둔다.

---

## 학습 순서

```
1. C 언어 (포인터, 비트 연산, volatile, 구조체)
2. STM32 GPIO (MODER, ODR, IDR 레지스터 직접 제어)
3. STM32 UART (printf 리다이렉트, 디버깅)
4. STM32 I2C (레지스터 직접, HAL 미사용)
5. STM32 CAN (루프백 테스트)
```

---

## 폴더 구조

```
TIL/
├── 00_필기노트/       ← 메인 커리큘럼
├── 10_학습자료/
│   ├── c언어/         ← C 언어 기초
│   ├── cs/            ← 임베디드수업, RTOS
│   ├── stm32/         ← STM32 주제별 노트
│   └── hardware/      ← 핀맵, 배선, 회로 자료
├── 20_면접대비/       ← 답변카드, 오답노트
├── 30_프로젝트/       ← 프로젝트 문서, 검증, 시뮬레이터, 도구
└── 90_템플릿/         ← 학습노트, 답변카드 템플릿
```

### 폴더 컨벤션

| 상황 | 위치 |
|------|------|
| 시리즈 강의 (Lesson N, #N) | `{카테고리}/{시리즈명}/N_제목.md` |
| 단일 학습 노트 | `{카테고리}/주제_소제목.md` |
| 답변카드 | `20_면접대비/답변카드/{개념명}.md` |

- 시리즈 폴더는 항상 `README.md`로 진도표 유지
- 진도표 행: `| N | [제목](파일.md) | 키워드 | [영상](URL) | 상태 |`

## 학습 → 면접 흐름

1. 원본 노트 작성 또는 외부 강의 정리
2. 학습노트 생성:
   - **기존 .md 정리:** `/learn-note <파일> [영상URL]` — 원문 보존 변환
   - **트랜스크립트만 있을 때:** `/learn-note 트랜스크립트 <영상URL>` + 본문 붙여넣기 — 청취 대체 자급자족 노트
3. `/make-card <노트>` → 답변카드 자동 추출
4. `/easy-quiz` → 답변카드 기반 부드러운 면접 연습

---

## AI한테 원하는 것

- 개념 설명, 질문 답변
- 코드 직접 짜주기 금지 — 내가 직접 짜야 포트폴리오 의미 있음
- 막혔을 때 힌트만
- 한국어로 소통

---

## 파일 네이밍 규칙

```
주제_소제목.md
예) 포인터_기초.md / 비트연산_레지스터설정.md / I2C_ACK_NACK.md
```

---

## 학습 노트 템플릿

- 새 학습 문서를 정리할 때는 `90_템플릿/학습노트.md` 형식을 기본 템플릿으로 사용한다.
- 주요 섹션은 `## 1. 제목` 형식으로 번호를 붙인다.
- 기본 콜아웃은 라벨 없이 사용한다.
- 실수하기 쉬운 내용과 예외만 `> **주의**`, `> **예외**` 라벨을 사용한다.
- 섹션 사이는 `---`로 구분한다.
- 마지막에는 `## 참고 자료`, `## 면접 답변` 섹션을 둔다.

### 기존 노트에 템플릿 적용 시 (옵션 A — 원문 보존)

- 원문 100% 보존이 기본. 임의 요약·삭제 금지
- 이모지는 모두 제거
- 템플릿 골격(섹션 번호, `---`, `참고 자료`, `면접 답변`)만 새로 적용
- 자동화: `/learn-note <파일> [영상URL]` 명령 사용

### 이미지 자리표시 (옵션 2 — 박스 placeholder)

다이어그램·스크린샷이 필요한 위치에 다음 형식 삽입:

```markdown
> **이미지 필요**
> {설명}
> - 출처: {데이터시트 페이지, 강의 타임스탬프}
> - 대체안: {대체 자료}
```

이미지 확보 시 `![설명](./images/N_파일명.png)`로 교체. 파일은 `{같은 폴더}/images/`에 저장.

---

## 커밋 규칙

```
Add: 새 학습 파일 추가     예) Add: GPIO_레지스터_직접제어
Fix: 내용 오류 수정        예) Fix: UART_보레이트_계산_오류
Update: 기존 파일 보완     예) Update: README 목차 추가
```

---

## 현재 진행 단계

> 이 줄을 직접 업데이트할 것

- [ ] C 언어
- [ ] GPIO
- [ ] UART
- [ ] I2C
- [ ] CAN
