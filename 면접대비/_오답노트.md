# 오답노트

`/easy-quiz` 실행 중 사용자가 모호하게 답하거나 못 떠올린 정확한 용어를 자동으로 축적합니다.
다시 마주칠수록 빈도가 올라가고, 자연스럽게 입에 붙습니다. 외우려 하지 않아도 됩니다.

---

## 자주 놓치는 정확 표현

| 모호한 표현 | 정확한 용어 | 출처 파일 | 빈도 |
|------------|-----------|----------|------|
| "최적화하지 말라고 시키기" | "객체의 읽기·쓰기를 실제 메모리 접근으로 유지하도록 강제" | 면접대비/답변카드/volatile_키워드.md | 1 |
| "캐싱 안 하고 매번 읽음" (읽기만 언급) | "읽기·쓰기 모두 실제 메모리 접근으로 유지" | 면접대비/답변카드/volatile_키워드.md | 1 |
| "const는 값이 변하지 않도록 강제" | "const는 내 코드가 이 값을 수정하지 않겠다는 약속 (외부 변경은 별개)" | 면접대비/답변카드/volatile_키워드.md | 1 |
| "volatile이 long에만 붙어있다" | "벤더 헤더 매크로에 volatile이 붙어있다 (long이라서가 아님)" | 면접대비/답변카드/unsigned_int_vs_unsigned_long.md | 1 |
| (막힘) 32비트 폭 보장 타입 못 떠올림 | "정확한 폭이 필요하면 stdint.h의 uint32_t" | 면접대비/답변카드/unsigned_int_vs_unsigned_long.md | 1 |
| "실행완료시간을 예측" | "task의 timing deadline을 예측 가능하게(deterministic) 만족" | 면접대비/답변카드/RTOS란_무엇인가.md | 1 |
| "임베디드시스템에서 써" (왜 쓰냐 질문에 대한 답) | "general OS는 non-deterministic이라 deadline 보장 못 함 → deadline miss = 치명적인 시스템에 필요" | 면접대비/답변카드/RTOS란_무엇인가.md | 1 |
| "인간의 책임" | "human responsiveness (사람 반응성)" | 면접대비/답변카드/RTOS란_무엇인가.md | 1 |
| (막힘) RTOS 30초 통합 답변 | "정의 → 공통점(OS 기능) → 차이점(deterministic) → 예시(engine/medical) → real-time 의미 정정" | 면접대비/답변카드/RTOS란_무엇인가.md | 1 |
| (막힘) RTOS 선택 5가지 기준 | "task 수 / 동시성 / timing deadline / resource 여유 / 팀 개발" | 면접대비/답변카드/RTOS_선택_기준.md | 1 |
| (막힘) RTOS 피하는 경우 | "task 적고 짧음 / 2KB RAM급 MCU / deadline 작업 1~2개 (ISR로 해결) / overhead 부담" | 면접대비/답변카드/RTOS_선택_기준.md | 1 |
| "오버헤드 모름" | "본래 목적 외 부수 비용. task switching의 context 저장·복원이 memory·CPU overhead" | 면접대비/답변카드/RTOS_선택_기준.md | 1 |
| medical 예시를 선택 기준 답변에 사용 | "medical은 RTOS 정의/필요성 예시. 선택 기준은 concurrent·기능분리·Wi-Fi/BT·팀개발 위주" | 면접대비/답변카드/RTOS_선택_기준.md | 1 |

---

## 사용 방법

- `/easy-quiz` 시 자동 갱신됨
- 직접 수정 가능
- 빈도 3 이상 = 다음 퀴즈에서 우선 출제
- `/easy-quiz 오답` 으로 이 노트 기반 미니 퀴즈

---

## 작성 규칙

- 사용자 답을 그대로 기록 (창피주기 X, 패턴 인식 O)
- 정확한 용어는 학습 노트 본문에서 추출
- 같은 모호 표현 중복 시 빈도 +1, 새 행 추가 X
